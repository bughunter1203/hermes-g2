{
  "_comment": "Copy to app.local.json (gitignored) and put your real bridge host here. Used only by `npm run pack:local`; the tracked app.json keeps its template hosts so the repo can stay public. Exact hosts only — no wildcards.",
  "networkWhitelist": [
    "wss://g2.your-host.example",
    "https://g2.your-host.example"
  ]
}
